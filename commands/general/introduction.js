const Discord = module.require(`discord.js`);

// YET TO FINISH

module.exports.run = async (bot, message, args) => {
    let introEmbed = new Discord.RichEmbed()
        

};

module.exports.help = {
    name: `introduction`
};